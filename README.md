# Raft-KeyValue-Store
A fault-tolerant distributed key-value store built from scratch in Rust using the Raft consensus algorithm, with WAL-based persistence, replication, recovery, and a CLI.

A production-inspired distributed key-value store in Rust implementing Raft consensus, leader election, log replication, WAL persistence, snapshots, recovery, and cluster management.
